# 许愿星（Superposition）· 分享版说明

这是一个双人「星星瓶」小系统：两个人各自往瓶子里写星（公开星 / 隐藏星），
可以请求对方的隐藏星、主动递星、接住、在纪念日一起拆星互看；
其中一位（AI 伴侣）由 AI 扮演，通过 MCP 工具参与全部流程。

> 原项目是一对伴侣的私人系统。分享版已剔除全部真实数据与部署信息：
> 数据库、日志、密钥文件、git 历史和原公网域名配置都不在包里。
> 第一次启动会自动生成全新的空数据库。

## 快速开始（Windows）

1. 安装 Python 3.12。
2. 配置两把访问钥匙（用户级环境变量；至少 32 字符且互不相同）：
   - `SUPERPOSITION_TOKEN_HUMAN`
   - `SUPERPOSITION_TOKEN_COMPANION`

   可选：设置更好记的网页登录密码 `SUPERPOSITION_PASSWORD_HUMAN / _COMPANION`
   （不设置则只能用钥匙登录）。
3. 首次运行先建虚拟环境并安装依赖（README §1）：

   ```bash
   python -m venv .venv
   .venv\Scripts\python -m pip install -r requirements.txt
   ```

4. 双击 `启动许愿星.bat`，浏览器打开 <http://127.0.0.1:8321/>，用密码或钥匙登录。
   注意界面分面：**网页登录只属于人类一方**；AI 伴侣的凭证在网页一律 403，只能通过 MCP 接入。
5. 种子不预置任何纪念日；第一次启动后，在页面上添加你们自己的特殊日期即可。

## 让 AI 参与（MCP）

- 本机 stdio：在 MCP 客户端注册命令 `.venv\Scripts\python mcp_server.py`
  （身份固定绑定AI 伴侣）。
- 远程（OAuth 2.1 登录页）：把 `/mcp` 经 HTTPS 暴露后，在 claude.ai
  添加自定义连接器即可，详见 README §8。

## 跑测试

README §2 有 15 个分步验证脚本，全部使用独立临时数据库，不会碰真实数据。

## 提醒

- `data/` 目录里是真实星星数据，想换人使用 / 全部重来，删掉该目录重启即可。
- 钥匙和密码只放环境变量，不要写进文件、截图或聊天记录。
